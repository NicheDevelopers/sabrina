services:
  nichebot:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./sabrina.db:/app/sabrina.db
      - ./logs:/app/logs
